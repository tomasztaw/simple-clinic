api:
  petStore:
    url: http://localhost:9999
  airQuality:
    url: http://localhost:9999

spring:
  jpa:
    hibernate:
      ddl-auto: validate

  security:
    enabled: false # wyłączenie security dla profilu testowego

logging:
  level:
    root: DEBUG
